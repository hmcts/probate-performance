plugins {
    id 'performance'
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

performance {
    simulationClass = 'simulations.Probate_Simulation'
    transactionNamesToGraph = ['Probate_390_DeclarationSubmit', 'Probate_440_ExecTwoDeclarationSubmit', 'Probate_600_LoadCitizenHub']
}
